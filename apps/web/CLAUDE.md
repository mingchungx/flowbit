@AGENTS.md

See the root `/CLAUDE.md` for full repo context and contribution guidelines.

This is the Next.js 16 app. API routes live in `src/app/api/`. Core business logic lives in `src/lib/core/`. Database schema is in `src/lib/db/schema.ts`. Chain integration is in `src/lib/chain/`.
